<h1><%= @tournament.name %></h1>
<h2>New Player</h2>

<%= render 'form', player: @player, url: tournament_players_path(@tournament.id) %>

<%= link_to 'Back', tournament_players_path(@tournament.id) %>
